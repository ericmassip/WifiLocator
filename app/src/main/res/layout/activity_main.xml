<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="edu.upc.eetac.dsa.wifilocator.MainActivity">

    <TextView
        android:id="@+id/wifiNetworksTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/wifiNetworksTitle"
        android:textSize="30sp" />

    <Button
        android:text="@string/showWifiNetworksBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:id="@+id/showWifiNetworksButton"
        android:onClick="showCurrentWifiNetworks" />

    <ListView
        android:id="@+id/wifiNetworks"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/showWifiNetworksButton"
        android:layout_below="@+id/wifiNetworksTitle"
        android:layout_alignParentStart="true" />

</RelativeLayout>
